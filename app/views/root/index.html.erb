<% if admin? %>
  <ul class="p-4">
    <li class="mb-4">
      <%= link_to("招待の管理", invitations_path, class: "inline-block w-full bg-blue-500 text-white p-2 text-center rounded") %>
    </li>
    <li class="mb-4">
      <%= link_to("Roleの管理", roles_path, class: "inline-block w-full bg-blue-500 text-white p-2 text-center rounded") %>
    </li>
  </ul>
<% else %>
  <p class="text-center mt-8 p-4">
    ここに招待コードを入力するフォームを配置する予定
  </p>
<% end %>

<p class="text-center mt-8 p-4">
  <%= link_to "ログアウト", session_path, data: { turbo_method: :delete }, class: "inline-block w-full border border-gray-200 py-2 rounded" %>
</p>
