<% content_for :title, "Roleの管理" %>

<table class="w-full">
  <tbody>
    <% @roles.each do |role| %>
      <tr>
        <td class="px-6 py-2">
          <%= role.name %>
        </td>
        <td class="px-6 py-2 text-right">
          <%= role.usable %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
